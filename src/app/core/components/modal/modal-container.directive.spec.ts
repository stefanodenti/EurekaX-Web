import { ModalContainerDirective } from './modal-container.directive';

describe('ModalContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalContainerDirective();
    expect(directive).toBeTruthy();
  });
});
